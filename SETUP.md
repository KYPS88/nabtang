# SETUP.md — คู่มือติดตั้ง MoneyLine ลง LINE + วิธีเอาไปเปิดขาย

คู่มือนี้เขียนแบบจับมือทำ ไม่ต้องเป็นโปรแกรมเมอร์ก็ทำตามได้
ทำครบแล้วจะได้: ลูกค้าแอดไลน์ → แตะเมนู → แอพ MoneyLine เปิดในไลน์ทันที

---

## ภาพรวม (ทำ 4 ขั้น)

1. เอาไฟล์แอพขึ้นเว็บ (ให้มีลิงก์ https)
2. สร้างบัญชี LINE OA (บัญชีไลน์ของธุรกิจ)
3. สร้าง LIFF app แล้วเอา **LIFF ID** มาใส่ในแอพ
4. เอาลิงก์แอพไปติดในเมนูของ LINE OA

---

## ขั้นที่ 1 — เอาไฟล์ขึ้นเว็บ (ฟรี ด้วย GitHub Pages)

แอพนี้เป็นไฟล์ HTML ธรรมดา แค่เอาขึ้นเว็บที่ไหนก็ได้ที่เป็น https
วิธีที่ง่ายและฟรีคือ GitHub Pages (repo นี้อยู่บน GitHub อยู่แล้ว):

1. เปิด repo นี้บน GitHub → **Settings** → เมนูซ้าย **Pages**
2. หัวข้อ Build and deployment → Source เลือก **Deploy from a branch**
3. Branch เลือก **main** / โฟลเดอร์ **/(root)** → กด **Save**
4. รอ 1-2 นาที รีเฟรชหน้า จะเห็นลิงก์เว็บ เช่น
   `https://<ชื่อผู้ใช้>.github.io/accountant/`
5. ทดสอบ: เปิด `https://<ชื่อผู้ใช้>.github.io/accountant/app.html` ในมือถือ
   ต้องเห็นแอพใช้งานได้ → เก็บลิงก์นี้ไว้ เรียกว่า **ลิงก์แอพ**

> ใช้บริการอื่นก็ได้ (Cloudflare Pages, Netlify, Vercel) ขอแค่เป็น https

## ขั้นที่ 2 — สร้าง LINE OA (บัญชีไลน์ธุรกิจ)

1. ไปที่ https://manager.line.biz → เข้าสู่ระบบด้วยบัญชีไลน์ส่วนตัว
2. กด **สร้างบัญชีใหม่ (Create new account)** → ตั้งชื่อ เช่น "MoneyLine"
   เลือกประเภทธุรกิจ กรอกข้อมูลตามฟอร์ม → ยืนยัน
3. เสร็จแล้วจะได้ LINE OA พร้อม LINE ID แบบ `@xxxxxxx`
   (ใช้ฟรีได้เลย แพ็กเกจฟรีก็พอสำหรับแอพนี้)

## ขั้นที่ 3 — สร้าง LIFF app แล้วเอา LIFF ID มาใส่

LIFF คือช่องทางที่ทำให้เว็บของเราเปิด "ในไลน์" ได้เหมือนเป็นแอพ

1. ไปที่ https://developers.line.biz/console → เข้าสู่ระบบบัญชีเดียวกัน
2. ถ้ายังไม่มี Provider: กด **Create a new provider** ตั้งชื่อ เช่น "MoneyLine"
3. กด **Create a new channel** → เลือกประเภท **LINE Login**
   - Region: Thailand · ตั้งชื่อ channel เช่น "MoneyLine App"
   - กรอกข้อมูลตามฟอร์ม → Create
4. เข้า channel ที่เพิ่งสร้าง → แท็บ **LIFF** → กด **Add**
   - **LIFF app name:** MoneyLine
   - **Size:** `Full` (เต็มจอ)
   - **Endpoint URL:** ลิงก์แอพจากขั้นที่ 1 (ลงท้าย `/app.html`)
   - **Scopes:** ติ๊ก `profile` (เพื่อทักทายด้วยชื่อจากไลน์)
   - กด Add
5. จะได้ **LIFF ID** หน้าตาแบบ `1234567890-AbcdEfgh` และ
   **ลิงก์ LIFF** แบบ `https://liff.line.me/1234567890-AbcdEfgh`
6. เอา LIFF ID ใส่ในแอพ ทำได้ 2 ทาง (เลือกทางเดียวพอ):
   - **ทางง่าย:** เปิดแอพ → กดปุ่ม ⚙️ มุมขวาบน → กรอก LIFF ID → บันทึก
   - **ทางถาวร (แนะนำตอนขายจริง):** แก้ไฟล์ `app.html` บรรทัด
     `const LIFF_ID = '';` ให้เป็น `const LIFF_ID = '1234567890-AbcdEfgh';`
     แล้วอัปโหลดขึ้นเว็บใหม่
     ⚠️ ถ้า repo เป็นสาธารณะ ไม่ควร commit LIFF ID จริง — ใช้ทางง่าย หรือทำ repo ส่วนตัว
7. ทดสอบ: ส่ง **ลิงก์ LIFF** เข้าแชทไลน์ตัวเอง → แตะ → แอพต้องเปิดในไลน์
   และหน้าแรกทักทายด้วยชื่อไลน์ของเรา

## ขั้นที่ 4 — ติดเมนูใน LINE OA

ให้ลูกค้าเจอแอพง่าย ๆ ด้วยเมนูใหญ่ใต้แชท (Rich menu):

1. กลับไปที่ https://manager.line.biz → เลือก OA ของเรา
2. เมนูซ้าย **ห้องแชท → เมนูลัด (Rich menu)** → สร้างใหม่
3. เลือกเทมเพลตแบบปุ่มเดียวหรือหลายปุ่นตามชอบ ใส่รูปปก
4. Action ของปุ่ม เลือก **ลิงก์ (Link)** → วาง **ลิงก์ LIFF** จากขั้นที่ 3
   ตั้งข้อความ เช่น "เปิด MoneyLine 💚"
5. กดใช้งาน → เสร็จ! ใครแอดไลน์ OA ก็แตะเมนูเปิดแอพได้เลย

---

## วิธีเอาไปเปิดขายให้ลูกค้า (ขายทีละราย)

เวอร์ชันนี้ข้อมูลเก็บในเครื่องของผู้ใช้แต่ละคน (localStorage)
จึงขายแบบ "ติดตั้งให้ลูกค้าคนละชุด" ได้ทันที โดยไม่ต้องมีเซิร์ฟเวอร์:

**ขายให้ลูกค้า 1 ราย = ทำ 3 อย่าง**
1. ก๊อปปี้ repo นี้เป็นของลูกค้า (Use this template / fork / อัปโหลดไฟล์ใหม่)
   จะเปลี่ยนโลโก้ ชื่อแอพ สีแบรนด์ ให้ลูกค้าก็แก้ใน `app.html` ได้
2. ทำขั้นที่ 1-4 ด้านบนด้วย **บัญชีของลูกค้า** (หรือของเราแล้วโอนสิทธิ์)
3. ส่งมอบ: ลิงก์ LIFF + สอนใช้ 10 นาที + เก็บเงิน 💰

**ไอเดียราคา** <เดาให้ — แก้ได้>
- ค่าติดตั้งครั้งแรก + ค่าดูแลรายปี (อัปเดตตารางภาษีทุกปีให้)
- หรือขายเป็นไลเซนส์รายปีผ่าน LINE OA ของเรา

**สิ่งที่ต้องทำทุกต้นปี (สำคัญ):**
ตรวจประกาศกรมสรรพากรของปีภาษีใหม่ แล้วอัปเดตตาราง `const TAX = {...}`
ใน `app.html` (จุดเดียว มีคอมเมนต์กำกับไว้แล้ว) — อัตราภาษี เพดานลดหย่อน
และมาตรการรายปี เช่น Easy E-Receipt

**ขั้นถัดไปเมื่อลูกค้าเยอะขึ้น:**
ต่อฐานข้อมูลกลาง (Supabase) + LINE Login เพื่อให้ข้อมูลผูกกับบัญชีไลน์
(ย้ายเครื่อง/ลบแชทแล้วข้อมูลไม่หาย) + ระบบสมัครสมาชิกเก็บเงินอัตโนมัติ
— โครงสร้างข้อมูลใน SPEC.md ออกแบบเผื่อไว้แล้ว

---

## แก้ปัญหาที่เจอบ่อย

| อาการ | วิธีแก้ |
|---|---|
| เปิดลิงก์ LIFF แล้วจอขาว | เช็ค Endpoint URL ต้องลงท้าย `/app.html` และเปิดได้จริงในเบราว์เซอร์ |
| ไม่ทักทายชื่อจากไลน์ | เช็คว่าใส่ LIFF ID แล้ว (⚙️ หรือใน app.html) และ scope `profile` ถูกติ๊ก |
| เปิดนอกไลน์แล้วพัง | ไม่ควรเกิด — แอพออกแบบให้ทำงานได้แม้ไม่มี LIFF ถ้าเจอให้แจ้งผู้พัฒนา |
| ข้อมูลลูกค้าหาย | localStorage หายได้ถ้าลบข้อมูลเบราว์เซอร์/ลบแชทไลน์ — แนะนำลูกค้ากดออก CSV เก็บสำรองเป็นระยะ |
