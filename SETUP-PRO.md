# SETUP-PRO.md — เปิดระบบ "นับตัง PRO" (บอทในแชท + ข้อมูลกลาง + ระบบขาย)

ทำตามนี้ครบแล้วจะได้:
- ลูกค้า**พิมพ์ในแชทไลน์** "ข้าวมันไก่ 60" หรือ**ส่งรูปสลิปโอนเงิน** → ระบบจดให้ + สรุปยอดเด้งกลับทันที
- ข้อมูล**ผูกกับบัญชีไลน์** เปลี่ยนมือถือ/ลบแชทก็ไม่หาย และเห็นตรงกันทั้งในแอพและในแชท
- ลูกค้าใหม่**ทดลองฟรี 14 วัน** → หมดแล้วกดซื้อ โอนพร้อมเพย์ ส่งสลิป → คุณพิมพ์อนุมัติในไลน์ → ปลดล็อกอัตโนมัติ

ใช้เวลาตั้งค่าประมาณ 45 นาที มี 4 ด่าน ทุกด่านมีของฟรี/ราคาถูกให้ใช้

---

## ด่าน 1 — สร้างฐานข้อมูลกลาง (Supabase — ฟรี)

1. ไปที่ https://supabase.com → Sign in ด้วย GitHub → **New project**
   - ตั้งชื่อ เช่น `nabtang` · ตั้งรหัสฐานข้อมูล (จดไว้) · Region เลือก Singapore (ใกล้ไทยสุด)
2. รอสร้างเสร็จ (~2 นาที) → เมนูซ้าย **SQL Editor** → **New query**
   → เปิดไฟล์ `supabase/schema.sql` ใน repo นี้ คัดลอกทั้งไฟล์มาวาง → กด **Run**
   → ขึ้น Success = ตารางครบแล้ว (users / transactions / payments)
3. เก็บกุญแจ 2 ตัว:
   - **Project URL**: เมนูซ้าย Settings → **Data API** → ช่อง Project URL
     เช่น `https://abcdxyz.supabase.co` → นี่คือ `SUPABASE_URL`
   - **Secret key**: เมนูซ้าย Settings → **API Keys** → หัวข้อ **Secret keys** →
     กดรูปตา/Copy ที่คีย์ `default` (ขึ้นต้น `sb_secret_...`) → นี่คือ `SUPABASE_SERVICE_KEY`
     (ระบบรองรับคีย์แบบเก่าด้วย: แท็บ "Legacy anon, service_role" → ใช้ service_role ก็ได้)
   ⚠️ Secret key เป็น**ความลับจริง** ห้ามใส่ในโค้ด ห้ามส่งให้ใคร — ใส่เฉพาะใน Railway (ด่าน 4)
   ส่วน Publishable key (`sb_publishable_...`) ไม่ต้องใช้ในระบบนี้

## ด่าน 2 — เปิดบอทรับแชท (LINE Messaging API — ฟรี)

1. ไปที่ https://developers.line.biz/console → เข้า Provider เดิม (นับตัง)
2. **Create a new channel** → เลือก **Messaging API** → ผูกกับ LINE OA เดิมของคุณ
   (ถ้าระบบพาไปสร้างที่ manager.line.biz ให้ทำตามจนจบแล้วกลับมาหน้า console)
3. ในหน้า channel Messaging API ที่ได้:
   - แท็บ **Basic settings** → **Channel secret** → นี่คือ `LINE_CHANNEL_SECRET`
   - แท็บ **Messaging API** → **Channel access token** กด Issue → นี่คือ `LINE_CHANNEL_ACCESS_TOKEN`
   - แท็บ **Messaging API** → **Webhook URL** ใส่ `https://nabtang-production.up.railway.app/webhook`
     → กด Verify (ตอนนี้อาจยัง error ได้ ถ้ายังไม่ทำด่าน 4 — ค่อยกลับมา Verify ซ้ำ) → เปิด **Use webhook**
4. ปิดข้อความตอบกลับอัตโนมัติของ OA (ไม่งั้นบอทเราตอบชนกับข้อความสำเร็จรูป):
   ที่ manager.line.biz → OA ของเรา → ตั้งค่า → การตอบกลับ → ปิด "ตอบกลับอัตโนมัติ" เปิด "Webhook"
5. **สำคัญมาก:** กลับไปที่ channel **LINE Login เดิม** → แท็บ LIFF → แอพนับตัง →
   ตรวจว่า **Scopes ติ๊กทั้ง `profile` และ `openid`** — ถ้าไม่ติ๊ก `openid`
   แอพจะผูกข้อมูลกับบัญชีไลน์ไม่ได้ (เปิดแอพแล้วข้อมูลไม่ sync) ติ๊กเพิ่มแล้วปิด-เปิดแอพใหม่
6. เก็บอีก 2 ค่า:
   - `LINE_LOGIN_CHANNEL_ID` = Channel ID ของ **channel LINE Login เดิม** (อันที่มี LIFF)
     ดูได้ที่ channel LINE Login → Basic settings → Channel ID (ตัวเลขล้วน)
   - `ADMIN_LINE_USER_ID` = User ID ของ**ตัวคุณเอง** (ไว้รับแจ้งเตือนสลิป + สิทธิ์พิมพ์อนุมัติ)
     ดูได้ที่ channel Messaging API → Basic settings → **Your user ID** (ขึ้นต้น U...)

## ด่าน 3 — กุญแจอ่านสลิป (Claude API — จ่ายตามใช้จริง ถูกมาก)

ระบบใช้ AI อ่านรูปสลิป (ยอดเงิน วันที่ บันทึกช่วยจำ) — ค่าใช้จ่ายราวสลิปละไม่ถึง 0.05 บาท

1. ไปที่ https://console.anthropic.com → สมัคร/ล็อกอิน → เติมเครดิตขั้นต่ำ ($5 ใช้ได้นานมาก)
2. เมนู **API Keys** → **Create Key** → คัดลอก → นี่คือ `ANTHROPIC_API_KEY`
   ⚠️ เป็นความลับจริงเช่นกัน — ใส่เฉพาะใน Railway
   (ไม่ใส่ค่านี้ก็ได้ — บอทยังจดจากการพิมพ์ได้ปกติ แค่อ่านสลิปไม่ได้)

## ด่าน 4 — ใส่กุญแจทั้งหมดใน Railway แล้วเปิดระบบ

1. เข้า https://railway.app → โปรเจกต์ nabtang → คลิก service → แท็บ **Variables**
2. กด **New Variable** เพิ่มทีละตัวตามตารางนี้:

| ชื่อตัวแปร | ค่า | จำเป็น? |
|---|---|---|
| `SUPABASE_URL` | จากด่าน 1 | ✅ |
| `SUPABASE_SERVICE_KEY` | จากด่าน 1 | ✅ |
| `LINE_CHANNEL_SECRET` | จากด่าน 2 | ✅ |
| `LINE_CHANNEL_ACCESS_TOKEN` | จากด่าน 2 | ✅ |
| `LINE_LOGIN_CHANNEL_ID` | จากด่าน 2 | ✅ |
| `ADMIN_LINE_USER_ID` | จากด่าน 2 | ✅ |
| `ANTHROPIC_API_KEY` | จากด่าน 3 | เฉพาะอ่านสลิป |
| `LIFF_URL` | `https://liff.line.me/2011686139-OUNayts9` | ✅ (บอทใช้ส่งลิงก์แอพ) |
| `PROMPTPAY_ID` | เบอร์มือถือ/เลขบัตร ปชช. พร้อมเพย์รับเงินของคุณ | ตอนเปิดขาย |
| `PRICE_THB` | ราคาต่อปี เช่น `199` | มีค่าเริ่มต้นให้ |
| `TRIAL_DAYS` | วันทดลองฟรี เช่น `14` | มีค่าเริ่มต้นให้ |

3. Railway จะรีสตาร์ตเอง → กลับไปด่าน 2 ข้อ 3 กด **Verify** ที่ Webhook URL ต้องขึ้น Success
4. **ทดสอบจริง:** ทักแชท OA ของคุณว่า `ข้าว 60` → บอทต้องตอบ "จดแล้ว ✓ ..." ภายใน 2-3 วินาที
   แล้วเปิดแอพจากลิงก์ LIFF → ต้องเห็นรายการ ข้าว 60 อยู่ในรายการ

---

## เช็คสถานะระบบ

เปิด `https://nabtang-production.up.railway.app/api/health` ในเบราว์เซอร์ —
จะบอกว่าตั้งค่าครบหรือยัง ขาดตัวไหน (ไม่โชว์ค่าความลับ)

## วิธีอนุมัติลูกค้าที่โอนเงิน

1. ลูกค้าหมดทดลองฟรี → แอพโชว์ QR พร้อมเพย์ + บอกให้ส่งสลิปในแชท
2. ลูกค้าส่งสลิป → คุณ (แอดมิน) ได้รับแจ้งในไลน์: ชื่อลูกค้า + รหัส + ยอด
3. เช็คเงินเข้าแล้วพิมพ์ในแชท OA: `อนุมัติ <รหัสลูกค้า>` เช่น `อนุมัติ NT4F2K`
   → ระบบปลดล็อก 1 ปี + แจ้งลูกค้าให้อัตโนมัติ (พิมพ์ `ปฏิเสธ <รหัส>` ได้เช่นกัน)

## ข้อจำกัดที่ควรรู้

- ข้อความ**ตอบกลับ** (ลูกค้าทักแล้วบอทตอบ) ฟรีไม่จำกัด · ข้อความที่บอท**ทักไปเอง** (เช่น แจ้งแอดมินเรื่องสลิป)
  ใช้โควตาข้อความ push ของแพ็กเกจ OA (แพ็กฟรีมีจำกัดต่อเดือน) — ระบบออกแบบให้ใช้ push น้อยที่สุดแล้ว
- การอ่านสลิปเป็นการประมาณค่าอัตโนมัติ ควรสุ่มตรวจความถูกต้องบ้างในช่วงแรก
